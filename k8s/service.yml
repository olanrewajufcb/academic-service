apiVersion: v1
kind: Service
metadata:
  name: academic-service
  labels:
    app: academic-service
spec:
  type: ClusterIP
  selector:
    app: academic-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8084
